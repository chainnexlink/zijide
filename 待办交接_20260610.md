# 待办交接 · 2026-06-10

> 本轮新增:介绍性优惠"展示代码" + 互助积分入钱包修复(已提交并推送到 `claude/appstore-ready`,**尚未部署**)。
> 下面三块按需处理。代码改动随**下个 App 构建**生效;后端两项需你部署。

---

## 一、当前状态(截至 2026-06-10)

- **App 1.0(7)**(黑屏修复版,iOS 26 实测通过)→ App 审核「**正在等待审核**」。等结果即可,**不要重新提交**(没代码变化、重提只会重新排队)。
- **介绍性优惠**(新用户首月五折 Personal $19.99 / Family $49.99,175 国)→ ✅ 早已在 ASC 配好、生效中。本轮补了 App 内"展示"代码,随下个构建上线。
- **两个订阅**(Personal/Family Monthly)→ ⏳ **没在这次审核里**(ASC 已知 bug 锁死),见第三块。

---

## 二、后端部署(等 1.0(7) 过审后再做,最稳;需你的 Supabase 登录)

> ⚠️ 顺序不能错:**先迁移,后函数**。`apple-iap` 函数会写 `is_intro_offer` 列,缺列会让购买验证报错。

**1) 数据库迁移**(Supabase 控制台 → SQL Editor 逐个粘贴运行):
- `migrations/20260610_subscription_order_intro_offer.sql` — 给 subscription_orders 加 `is_intro_offer` 列
- `migrations/20260610_mutual_aid_points_to_wallet.sql` — 加原子入账函数 `credit_user_points`(仅 service_role 可调)
- (如还没跑过)`migrations/20260601_mutual_aid_antiabuse.sql` — 互助防刷分触发器

**2) 边缘函数**(终端里先 `supabase login` 再 `supabase link --project-ref aurowjqmjofpitsmlhmg`):
```
supabase functions deploy apple-iap     # 必须在①之后
supabase functions deploy mutual-aid
```

**3) 验证**:购买一次能正常开通;互助完成一次救援后,`/points` 积分页能看到 earn_rescue 流水、余额增加。

---

## 三、订阅卡死处理(等审核结果后再动;现在别撤审)

**性质**:App Store Connect **已知 bug**——App 被拒后,首个订阅卡「需要开发者操作」、Submit 按钮变灰、版本页常不出现"App 内购买项目和订阅"选择区、改订阅也不解锁。

**按优先级试(需手动 / 趁 ASC 不抽风)**:
1. **手动改本地化**:逐个订阅 → 英文本地化 → 手敲改一个字符再保存 → 常翻回"准备提交"。
2. **刷新抢点**:订阅页刷新后,趁按钮没变灰立刻点"添加以供审核"。
3. **联系 Apple 开发者支持**(官方可靠解)→ 用下面的工单草稿。
4. ❌ 别用"新建订阅"(要换 product ID、得改 App 代码)。

### Apple 开发者支持工单草稿(复制到 App Store Connect → Contact Us)

```
Subject: First auto-renewable subscriptions stuck in "Developer Action Needed" after app rejection

App: WarRescue: Air-Raid Alerts
Apple ID: 6774955063
Bundle ID: com.warrescue.app

Hello,

After our app version 1.0 was rejected (Guideline 2.1), our two first
auto-renewable subscriptions are stuck:

- Personal Monthly (com.warrescue.app.personal.monthly)
- Family Monthly (com.warrescue.app.family.monthly)

Both show status "Developer Action Needed" and the "Submit for Review"
button is greyed out. The "In-App Purchases and Subscriptions" selection
section does not appear on our app version page, so we cannot attach these
first subscriptions to a new app version submission. Editing the
subscription localizations does not re-enable the button.

We have since uploaded a fixed build (1.0 build 7) which is currently
"Waiting for Review". Please help us submit these two first subscriptions
for review together with our app, or advise the correct steps to unstick
them.

Thank you.
```

---

---

## 四、闭环审计修复(2026-06-10,11 条用户闭环逐条追踪后)

**已在代码/迁移层修闭(生效仍需跑补丁 + 部署函数):**
1. 🔴 **家庭组建不起来** → `family_members` 补 INSERT/DELETE RLS 策略(此前只有 SELECT,成员行被静默拒)。
2. 🔴 **互助积分进不了钱包** → 真因是 `user_points` 表 + `credit_user_points` 函数压根没进部署脚本。已合进下方补丁。
3. 🟠 **积分抵扣订阅(失诺)** → 合规改造为「**积分兑换订阅天数**」:新增 `redeem-points` 边缘函数 + 订阅页入口(500分=7天/2000分=30天);不降 Apple 实付价(避免违反 3.1.1)。
4. 🟠 **避难收藏不保存 / 纠错静默丢失** → 建 `favorite_shelters`、`shelter_reports` 表;ShelterDetail 改写对的表并据返回判成败。
5. 🟠 **离线导航卡死** → NavigationPage 断网/取不到路线时切换离线地图 + 直线距离 + 明确提示,不再无限「加载中」。
6. 🔴 **SOS 兜底静默不发通知** → Dashboard 兜底路径加明确告警,提醒用户直接电话联系紧急联系人。

**部署增量(在第二块基础上加):**
- **数据库**:整段跑 **`数据库补丁_20260610.sql`**(幂等,含积分表/函数 + 家庭RLS + 收藏/纠错表 + 订单审计列)。这一条等于把前面零散迁移都覆盖了。
- **边缘函数**:`supabase functions deploy redeem-points`(新增);`mutual-aid`、`apple-iap` 照旧。
- **仍是配置项(本轮没动)**:APNs/FCM 密钥、.p8 促销密钥、预警 cron 的 `app.settings`(任务#14)——见第二/三块。

**审计判为 OK 的闭环**:注册登录+试用、Apple 订阅购买、模拟演练(真闭合);推送、推荐返券(代码闭合、缺密钥)。

---

## 备注
- 本轮 commit:`e3ba043`(介绍性优惠展示)、`d067c33`(积分入钱包),已推送 `claude/appstore-ready`。
- 工作区里的 `verify-iap.js` 是早先未跟踪文件,非本轮改动,未提交。
