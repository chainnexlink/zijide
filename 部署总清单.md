# 部署总清单（P1–P4 一次性上线）

> 按顺序执行。每个 `[ ]` 是一个可勾选步骤。报错就把输出发我。各阶段细节见对应 `Pn_*_部署说明.md`。

## 0. 前置
- [ ] 安装并登录 Supabase CLI：`supabase login`，`supabase link --project-ref aurowjqmjofpitsmlhmg`
- [ ] **敲定真实 Bundle ID**：当前 `capacitor.config.json` 的 `com.warrescue.appname` 像占位符。确定 App Store Connect 上的真实 bundle id，保证三处一致：`capacitor.config.json` appId、Xcode、以及下面的 `APNS_BUNDLE_ID` / `APPSTORE_BUNDLE_ID`。

## 1. 数据库迁移
- [ ] 应用本次新增迁移（`supabase db push`，或在 SQL Editor 逐个执行）：
  - `migrations/20260529_alert_pipeline_realdata.sql`（P1：预警表修复 + cron）
  - `migrations/20260529_device_tokens.sql`（P2：设备 token 表）
  - `migrations/20260529_admin_auth.sql`（P4：admin_users + RLS）
  - `migrations/20260530_user_location.sql`（地理过滤：user_alert_settings 加最近坐标列。**必须跑**，否则预警/SOS 推送会因查不到坐标列而静默发给空集）
- [ ] P1 定时任务 GUC（SQL Editor 跑一次，换成真实值）：
  ```sql
  alter database postgres set app.settings.functions_url = 'https://aurowjqmjofpitsmlhmg.supabase.co/functions/v1';
  alter database postgres set app.settings.cron_secret   = '<与 CRON_SECRET 一致的随机串>';
  ```

## 2. Edge Functions 部署
- [ ] `supabase functions deploy ai-alert`
- [ ] `supabase functions deploy monitor`
- [ ] `supabase functions deploy push-dispatch`
- [ ] `supabase functions deploy sos-service`
- [ ] `supabase functions deploy apple-iap`
  > `functions/_shared/*` 会随引用它的函数自动打包。

## 3. Secrets（`supabase secrets set KEY=VALUE`）
- [ ] **P1**：`CRON_SECRET=<随机串>`；可选 `MEOO_PROJECT_API_KEY=<AI密钥>`
- [ ] **务必不要**设 `ALERT_DEMO_MODE`（设了就会出假预警）
- [ ] **P2 iOS**：`APNS_KEY_ID` `APNS_TEAM_ID` `APNS_BUNDLE_ID` `APNS_AUTH_KEY`（.p8 全文）
- [ ] **P2 安卓**：`FCM_SERVICE_ACCOUNT`（服务账号 JSON 全文）
- [ ] **P3 Twilio**：`TWILIO_ACCOUNT_SID` `TWILIO_FROM_NUMBER` `TWILIO_KEY_SID` `TWILIO_KEY_SECRET`
- [ ] **P4 内购**：`APPSTORE_ISSUER_ID` `APPSTORE_KEY_ID` `APPSTORE_BUNDLE_ID` `APPSTORE_PRIVATE_KEY`（.p8 全文）

## 4. 引导超级管理员（否则进不去后台）
- [ ] 用你的邮箱在 App 注册/登录一次（或 Supabase 控制台 Authentication → Add user）
- [ ] SQL Editor 执行（换成你的邮箱）：
  ```sql
  INSERT INTO public.admin_users (user_id, email, display_name, role)
  SELECT id, email, '超级管理员', 'superadmin' FROM auth.users WHERE email = 'YOUR_EMAIL@example.com'
  ON CONFLICT (user_id) DO UPDATE SET role = 'superadmin';
  ```

## 5. 客户端构建出包
- [ ] `.env` 填新轮换的 `GOOGLE_MAPS_KEY=`（旧 key 去 GCP 删除/限制）
- [ ] 安卓：把 Firebase 的 `google-services.json` 放到 `android/app/`
- [ ] `npm run build`（前端 dist/）、`npm run admin:build`（后台 dist-admin/）
- [ ] `npx cap sync`
- [ ] iOS：Xcode 打开 `ios/App`，开启 Push Notifications capability → Archive
- [ ] 安卓：Android Studio 出 AAB

## 6. 冒烟验证（部署后逐项确认）
- [ ] `POST .../functions/v1/ai-alert?action=collect` 返回 `demoMode:false` 且写入了真实预警；`alert_crawl_state` 有记录
- [ ] 等几分钟，`alert_crawl_state.last_crawl_at` 自动更新（cron 在跑）
- [ ] 真机：App 关屏/锁屏时仍收到预警推送
- [ ] 真机：触发 SOS → 紧急联系人收到带定位的短信；家人/互助设备收到推送
- [ ] 后台：用超管邮箱+密码登录成功；用非管理员账号登录被拒
- [ ] 内购：正常购买能开通订阅；构造一个假的 jwsTransaction 调 `apple-iap` 被拒（`Transaction could not be verified with Apple`）

---

## 7. 部署后数据初始化（一次性）
- 后台登录后 → 「救援组织管理」点 **导入内置数据**（救援组织入库），「避难所管理」点 **导入内置数据**（避难所入库，幂等可重复点）。之后这些数据在后台正常增删改。

## 8. 安全收尾（建议；非上架硬卡点）
- 运行 `rls_安全审计.sql`（只读）导出线上 RLS 策略 → 把结果发我，我写精确的收紧迁移（盲改会弄坏线上 App，故先审计后收紧）。

## 9. 合规材料
- 见 `合规与上架材料.md`：iOS 隐私清单已生成（**需在 Xcode 勾 Target Membership**）、出口合规已设、订阅页已核查、数据安全申报对照表、Critical Alerts 申请话术、战争内容审核备注。
