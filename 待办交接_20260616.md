# WarRescue 闭环修复交接 · 2026-06-16

> 本轮按「A 修真 bug → B 补后台管理 → C 交接」执行。所有改动在分支 `claude/appstore-ready`，
> **不影响正在审核的 1.0(8) 包**（admin 是独立网页后台、不进 iOS 包；src 改动只进下个版本，未重打包）。

---

## ⚡ 0. 立即要做的两件事（否则本轮改动不生效）

1. **跑数据库补丁**：在 Supabase SQL Editor（项目 `aurowjqmjofpitsmlhmg`）整段粘贴运行
   **`数据库补丁_20260616.sql`**（幂等，可重复跑）。
   - 前置：需先跑过 `20260529_admin_auth.sql`（提供 `is_admin()`）与 `数据库补丁_20260610.sql`。
2. **重新构建并部署网页后台**：`npm run admin:build` 后把产物部署到后台托管处。
   （新「纠错反馈」页在前端；读数据靠补丁里的管理员 RLS 策略，缺一不可。）

---

## ✅ 1. 本轮已完成（已提交、已通过 tsc 双工程类型检查）

### A. 真 bug / 数据错配修复
- **公告类型约束错配**（坐实的真 bug）：`announcements.type` 的 CHECK 只允许
  `info/warning/critical/update`，但后台「公告管理」默认发 `type='promotion'`（还有 `reward`），
  且 in-app 后台 `saveAnnouncement` **不检查 error** → 促销/奖励公告被 CHECK(23514) 静默拒绝、
  用户端永远看不到。生产实测：announcements 表里只有 1 条 `info`，印证从没成功发过促销公告。
  - 修：`数据库补丁_20260616.sql` 动态删旧 CHECK、加覆盖全部取值的新约束
    （info/warning/critical/update/reward/promotion）。
  - 修：`admin/src/pages/ContentMgmt.tsx` 类型下拉的孤儿值 `emergency` → `critical`（前端 Banner 只认 critical）。
  - 修：`src/pages/AdminDashboard.tsx` `saveAnnouncement` 增加 error 显形（保存失败不再假装成功）。

### B. 补缺失的后台管理：避难所纠错反馈审核
- 背景：用户在 App「避难所详情」提交纠错 → 写入 `shelter_reports`，但**全后台无任何入口** →
  反馈进表却无人处理（闭环断裂；这是上一轮新开的写入路径，本轮补上读+处理端）。
- 新增：admin 后台「避难所管理 → 纠错反馈」页（`admin/src/pages/ShelterMgmt.tsx` 的 `ShelterReportsView`），
  支持按状态筛选（待处理/处理中/已解决/已驳回）+ 一键改状态 + 重开。
- 配套：`数据库补丁_20260616.sql` 给 `shelter_reports`（及 `favorite_shelters`/`user_points`/`point_transactions`，
  均为 admin_auth 迁移**之后**才新建、漏配管理员策略的表）补 `is_admin()` 的 `FOR ALL` 策略，
  与 `20260529_admin_auth.sql` 完全同模式，否则后台读不到。

---

## 🔍 2. 核实结论（这几条「疑似 bug」经生产实测被排除，**不要再去改**）
- **geo 列**：`user_alert_settings.last_latitude/last_longitude/location_updated_at` 生产**已存在**
  （`20260530_user_location.sql` 已跑过；REST 探针返回 200 而非 42703）。ai-alert 地理过滤正常，非 bug。
- **rescue_organizations**：生产表**已存在**且有后台页（RescueOrgMgmt），非缺口。
- **notifications / family_notifications**：表存在、`ai-alert` 会写入，但前端**没有任何读取组件**
  （i18n 备好了「通知中心」文案，页面没建）。属半成品功能，非崩溃 bug → 见下方 P3。

---

## 📋 3. 剩余「实体→后台管理」缺口（按优先级，供下一步排期）

> 来源：对两个后台（in-app `src/pages/AdminDashboard.tsx` + 独立 `admin/` 站点 11 页）的逐实体核查。
> 大多数实体后台管理其实**已完整**（预警/避难所/SOS/客服/家庭/订阅查看/积分发放/公告等都齐）。仅以下有缺：

| 优先级 | 缺口 | 现状 | 建议 |
|---|---|---|---|
| **P1** | 订阅**退款**工具 | SubscriptionMgmt 只能「取消」，无退款 | 接 App Store Server API 退款查询/记录 + `subscription_refunds` 表 |
| **P1** | 用户**封禁/停用/角色** | UserCenter 只有「删除用户」 | 加 banned 标记 + RLS 拦截 + 角色升降 |
| P2 | 推荐/邀请**反欺诈审计** | UserCenter「邀请管理」只读 | 加撤销返券/标记欺诈/校验邀请人 |
| P2 | 互助**响应审批** | MutualAid 看得到响应、无批/拒 | 加资质审核/拒绝工作流 |
| P2 | 同城预警**反刷分** | AlertCenter 发奖无反作弊 | 同用户/同时段重复上报去重 |
| P3 | **通知中心**（前端） | notifications 表只写不读 | 建用户端通知列表页读该表（或确认仅用系统推送、停写该表） |
| P3 | city_alert_summaries **聚合链路** | 后台能读/发奖，但 summary 行由谁 INSERT 待核实 | 核实触发器/函数是否生成 summary，否则后台列表恒空 |

---

## 🔑 4. 仍待你配置/部署的项（沿用既有交接，未变）
- APNs / FCM 推送密钥、`.p8` 促销优惠签名密钥（填进 GitHub Secret 后重跑 `supabase-deploy.yml`）。
- 预警定时采集的 `app.settings`（`ALTER DATABASE`，见 deploy_state 备忘）。
- 若 `数据库补丁_20260610.sql` 尚未跑，请一并跑（积分钱包/家庭 RLS/收藏/纠错表/客服写策略）。

---

## 🍎 5. App Store 状态
- **1.0(8) 正在审核**。本轮全部改动（admin 网页 + src 下个版本代码）**不进该在审包**，不会干扰审核。
- 审核结果出来后：通过→协助发布；被拒→按拒因继续修。
- ⚠️ 审核期间勿用 `+852 51401770` 走短信登录（会轮换密码、作废 ASC 里填的演示账号密码 `WarDemo2026!`）。
