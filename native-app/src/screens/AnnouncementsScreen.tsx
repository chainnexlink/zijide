import { useCallback, useEffect, useState } from 'react';
import { ActivityIndicator, Pressable, StyleSheet, Text, View } from 'react-native';
import type { NativeStackScreenProps } from '@react-navigation/native-stack';
import { Screen } from '../components/Screen'; import { supabase } from '../lib/supabase'; import { colors, radius, spacing } from '../theme'; import type { RootStackParams } from '../navigation/RootNavigator';
type Props = NativeStackScreenProps<RootStackParams, 'Announcements'>; type Row = { id: string; title: string; content: string; type: string; created_at: string };
export function AnnouncementsScreen({ navigation }: Props) { const [rows, setRows] = useState<Row[]>([]); const [loading, setLoading] = useState(true); const load = useCallback(async () => { const { data } = await supabase.from('announcements').select('id,title,content,type,created_at').eq('is_active', true).order('created_at', { ascending: false }); setRows((data || []) as Row[]); setLoading(false); }, []); useEffect(() => { void load(); }, [load]); if (loading) return <View style={styles.loading}><ActivityIndicator color={colors.info} /></View>; return <Screen title="???????" subtitle="?????????" action={<Pressable onPress={() => navigation.goBack()}><Text style={styles.back}>??</Text></Pressable>} refreshing={loading} onRefresh={() => void load()}>{rows.map((row) => <Pressable key={row.id} style={styles.card} onPress={() => navigation.navigate('AnnouncementDetail', { announcement: row })}><Text style={[styles.type, { color: row.type === 'critical' ? colors.danger : row.type === 'warning' ? colors.warning : colors.info }]}>{row.type.toUpperCase()}</Text><Text style={styles.title}>{row.title}</Text><Text style={styles.preview} numberOfLines={3}>{row.content}</Text><Text style={styles.date}>{new Date(row.created_at).toLocaleString('zh-CN')}</Text></Pressable>)}{!rows.length ? <Text style={styles.empty}>????</Text> : null}</Screen>; }
const styles = StyleSheet.create({ loading: { flex: 1, backgroundColor: colors.background, alignItems: 'center', justifyContent: 'center' }, back: { color: colors.info, fontWeight: '800' }, card: { backgroundColor: colors.surface, borderColor: colors.border, borderWidth: 1, borderRadius: radius.md, padding: spacing.lg, gap: spacing.sm }, type: { fontSize: 11, fontWeight: '900' }, title: { color: colors.text, fontSize: 19, fontWeight: '900' }, preview: { color: '#CBD5E1', lineHeight: 21 }, date: { color: colors.muted, fontSize: 11 }, empty: { color: colors.muted, textAlign: 'center', paddingVertical: 60 } });
