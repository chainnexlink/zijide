import { Pressable, Share, StyleSheet, Text, View } from 'react-native'; import type { NativeStackScreenProps } from '@react-navigation/native-stack'; import { Screen } from '../components/Screen'; import { colors, radius, spacing } from '../theme'; import type { RootStackParams } from '../navigation/RootNavigator';
type Props = NativeStackScreenProps<RootStackParams, 'NewsDetail'>; export function NewsDetailScreen({ route, navigation }: Props) { const article = route.params.article; const body = article.content.replace(/<\/?(h\d|p|ol|ul|li|br)[^>]*>/gi, '\n').replace(/<[^>]*>/g, '').replace(/\n{3,}/g, '\n\n').trim(); return <Screen title="????" subtitle={`${article.author || 'WarRescue'} ? ${new Date(article.published_at).toLocaleDateString('zh-CN')}`} action={<Pressable onPress={() => navigation.goBack()}><Text style={styles.back}>??</Text></Pressable>}><View style={styles.card}><Text style={styles.title}>{article.title}</Text><Text style={styles.tags}>{article.tags.join(' ? ')}</Text><Text style={styles.body}>{body}</Text></View><Pressable style={styles.share} onPress={() => void Share.share({ message: `${article.title}\n\n${body}` })}><Text style={styles.shareText}>??????</Text></Pressable><Text style={styles.note}>??????????????????????????</Text></Screen>; }
const styles = StyleSheet.create({ back: { color: colors.info, fontWeight: '800' }, card: { backgroundColor: colors.surface, borderColor: colors.border, borderWidth: 1, borderRadius: radius.md, padding: spacing.lg, gap: spacing.md }, title: { color: colors.text, fontSize: 25, fontWeight: '900' }, tags: { color: colors.info, fontWeight: '800' }, body: { color: '#E2E8F0', lineHeight: 24 }, share: { height: 48, borderColor: colors.info, borderWidth: 1, borderRadius: radius.md, alignItems: 'center', justifyContent: 'center' }, shareText: { color: colors.info, fontWeight: '900' }, note: { color: colors.muted, textAlign: 'center', fontSize: 11, lineHeight: 17 } });
