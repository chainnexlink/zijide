import { Pressable, StyleSheet, Text, View } from 'react-native'; import Constants from 'expo-constants'; import type { NativeStackScreenProps } from '@react-navigation/native-stack'; import { Screen } from '../components/Screen'; import { colors, radius, spacing } from '../theme'; import type { RootStackParams } from '../navigation/RootNavigator';
type Props = NativeStackScreenProps<RootStackParams, 'About'>;
export function AboutScreen({ navigation }: Props) { return <Screen title="关于 WarRescue" subtitle="原生紧急预警与救援 App" action={<Pressable onPress={() => navigation.goBack()}><Text style={styles.back}>返回</Text></Pressable>}><View style={styles.hero}><Text style={styles.logo}>WR</Text><Text style={styles.name}>WarRescue</Text><Text style={styles.version}>版本 {Constants.expoConfig?.version || '1.1.0'}</Text></View><View style={styles.card}><Text style={styles.title}>我们的使命</Text><Text style={styles.body}>在战争、灾害和通信受限环境中，把可信预警、SOS、家人守护、互助救援、避难所和安全路线集中在一个原生 App 中。</Text><Text style={styles.body}>订阅收入的 10% 计划用于战后重建与人道主义援助。具体执行记录将通过 App 公告披露。</Text></View><View style={styles.card}><Text style={styles.title}>重要说明</Text><Text style={styles.body}>WarRescue 不能替代当地政府、民防、警察、消防和医疗急救。任何情况下都应优先遵循官方命令并保证自身安全。</Text></View></Screen>; }
const styles = StyleSheet.create({ back: { color: colors.info, fontWeight: '800' }, hero: { alignItems: 'center', backgroundColor: colors.surface, borderRadius: radius.lg, padding: spacing.xl, gap: spacing.sm }, logo: { width: 72, height: 72, borderRadius: 22, textAlign: 'center', lineHeight: 72, backgroundColor: colors.danger, color: colors.white, fontSize: 24, fontWeight: '900' }, name: { color: colors.text, fontSize: 27, fontWeight: '900' }, version: { color: colors.muted }, card: { backgroundColor: colors.surface, borderColor: colors.border, borderWidth: 1, borderRadius: radius.md, padding: spacing.lg, gap: spacing.md }, title: { color: colors.text, fontSize: 18, fontWeight: '900' }, body: { color: '#CBD5E1', lineHeight: 23 } });
