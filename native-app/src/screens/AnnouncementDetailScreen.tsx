import { Pressable, Share, StyleSheet, Text, View } from 'react-native'; import type { NativeStackScreenProps } from '@react-navigation/native-stack'; import { Screen } from '../components/Screen'; import { colors, radius, spacing } from '../theme'; import type { RootStackParams } from '../navigation/RootNavigator';
type Props = NativeStackScreenProps<RootStackParams, 'AnnouncementDetail'>;
export function AnnouncementDetailScreen({ route, navigation }: Props) { const row = route.params.announcement; return <Screen title="????" subtitle={new Date(row.created_at).toLocaleString('zh-CN')} action={<Pressable onPress={() => navigation.goBack()}><Text style={styles.back}>??</Text></Pressable>}><View style={styles.card}><Text style={styles.title}>{row.title}</Text><Text style={styles.body}>{row.content.replace(/<[^>]*>/g, '')}</Text></View><Pressable style={styles.share} onPress={() => void Share.share({ message: `${row.title}\n\n${row.content.replace(/<[^>]*>/g, '')}` })}><Text style={styles.shareText}>????</Text></Pressable></Screen>; }
const styles = StyleSheet.create({ back: { color: colors.info, fontWeight: '800' }, card: { backgroundColor: colors.surface, borderColor: colors.border, borderWidth: 1, borderRadius: radius.md, padding: spacing.lg, gap: spacing.lg }, title: { color: colors.text, fontSize: 25, fontWeight: '900' }, body: { color: '#E2E8F0', lineHeight: 24 }, share: { height: 48, borderColor: colors.info, borderWidth: 1, borderRadius: radius.md, alignItems: 'center', justifyContent: 'center' }, shareText: { color: colors.info, fontWeight: '900' } });
